export default class Hole {
    constructor(cup, walls, zones, slopes, startX, startY, par) {
        this.cup = cup;
        this.walls = walls;
        this.zones = zones;
        this.slopes = slopes;
        this.startX = startX;
        this.startY = startY;
        this.par = par;
    }
}