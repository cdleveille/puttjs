export default class Hole {
    constructor(cup, walls, zones, startX, startY, par) {
        this.cup = cup;
        this.walls = walls;
        this.zones = zones;
        this.startX = startX;
        this.startY = startY;
        this.par = par;
    }
}